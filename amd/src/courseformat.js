import L from 'core/log';
export const sections = () => {
    L.debug("here we are playing with sections");
    // let section = document.querySelector('.course-content li.section .course-section-header');
    // if (section.dataset.number < 6) {
    //     section.classList.remove('draggable');
    //     L.debug(section);
    //     L.debug("Don't drag me down " + section.dataset.number);
    // }

};